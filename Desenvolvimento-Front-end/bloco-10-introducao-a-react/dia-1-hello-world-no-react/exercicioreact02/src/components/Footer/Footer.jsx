import React, { Component } from 'react';
import { TextFooter } from './styles';

class Footer extends Component {
    render(){
        return(
            <TextFooter>E isso é só o começo...</TextFooter>
        )
    }
}

export default Footer;